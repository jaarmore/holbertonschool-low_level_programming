#include "holberton.h"
/**
 * add - function that adds two integers and return result.
 * @num1: first number to add
 * @num2: second number to add
 * Return: result of adds num1 + num2
*/
int add(int num1, int num2)
{
	int sum = 0;

	sum = num1 + num2;

	return (sum);
}
