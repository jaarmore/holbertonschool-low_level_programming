#include <stdio.h>
/**
 * main - entry point.
 * @argc: number of arguments.
 * @argv: String of characters
 * Return: always return 0.
*/
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
