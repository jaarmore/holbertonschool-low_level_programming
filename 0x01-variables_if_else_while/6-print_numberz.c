#include <stdio.h>
/**
 * main - prints all single numbers starting from 0
 * Return: always return 0
*/
int main(void)
{
	int num = 48;

	while (num <= 57)
	{
		putchar(num);
		num++;
	}
	putchar(10);
	return (0);
}
